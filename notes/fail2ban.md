# Fail2ban

Purpose:
Protects services such as SSH by blocking IP addresses after repeated failed login attempts.

Commands Used

sudo apt install fail2ban

sudo systemctl status fail2ban

sudo fail2ban-client status
